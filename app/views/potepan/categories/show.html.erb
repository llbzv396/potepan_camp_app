<!-- SITE TITTLE -->
<% provide(:title, @taxon.name) %>
<%= render partial: 'shared/page_header', locals: { title: @taxon.name, active: @taxon.name } %>
<section class="mainContent clearfix productsContent">
  <div class="container">
    <div class="row">
      <div class="col-md-3 col-sm-4 col-xs-12 sideBar">
        <%= render partial: 'filter_by_taxon', locals: { taxonomies: @taxonomies } %>
        <%= render 'filter_by_price' %>
        <%= render 'filter_by_color' %>
        <%= render 'filter_by_size' %>
      </div>
      <%= render partial: 'products_index', locals: { products: @products } %>
    </div>
  </div>
</section>
<%= render 'shared/bottom_light' %>
