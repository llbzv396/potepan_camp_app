<!-- SITE TITTLE -->
<% provide(:title, @taxons.name) %>
<%= render 'page_header' %>
<section class="mainContent clearfix productsContent">
  <div class="container">
    <div class="row">
      <div class="col-md-3 col-sm-4 col-xs-12 sideBar">
        <%= render 'taxonomies' %>
        <%= render 'filters' %>
      </div>
      <%= render 'products' %>
    </div>
  </div>
</section>
<%= render 'bottom_light' %>
