<!-- SITE TITTLE -->
<% provide(:title, @product.name) %>
<!-- LIGHT SECTION -->
<%= render partial: 'shared/page_header', locals: { title: @product.name, active: @product.name } %>
<!-- MAIN CONTENT SECTION -->
<section class="mainContent clearfix">
  <div class="container">
    <%= render 'single_product' %>
    <%= render 'related_products_index' %>
  </div>
</section>
