<% if object.errors.any? %>
  <div class="alert alert-danger">
    正しく入力されていないフォームが<%= object.errors.count %>箇所あります
  </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <strong><li class="text-danger"><%= msg %></li></strong>
      <% end %>
    </ul>
<% end %>
